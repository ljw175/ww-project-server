export const selectWorlds = state => state.world.worlds;
export const selectSelectedWorldName = state => state.world.selectedWorldName;
export const selectIsEditMode = state => state.world.isEditMode;
export const selectButtonClickStatus = state => state.world.buttonClickStatus;
